<!DOCTYPE html>
<html>
  <head>
    <title>TwitterProject</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css'%>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <h5 class="my-0 mr-md-auto font-weight-normal">Twitter</h5>
      <nav class="my-2 my-md-0 mr-md-3">
        <span class="p-2 text-dark"><%= link_to 'На главную',home_path %></span>
      </nav>
      
      <% if user_signed_in? %>
        <button class="btn btn-outline-primary"><%= link_to 'Новый пост', new_post_path %></button>
        <button class="btn btn-outline-primary"><%= link_to 'Выйти', destroy_user_session_path %></button>

      <% else %>
        <button class="btn btn-outline-primary"><%= link_to 'Зарегистрироваться', new_user_registration_path %></button>
        <button class="btn btn-outline-primary"><%= link_to 'Войти', new_user_session_path %></button>
      <% end %>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <% flash.each do |name, message| %>
            <div class="alert <% bootstrap_class_for(name)%>">
              <button class="close" data-dismiss="alert">&times;</button>
              <% message %>
            </div>
          <%end%>
        </div>
      </div>

      <%= yield %>
    </div>
  </body>
</html>
